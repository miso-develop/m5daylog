# Optional credential for gated model acquisition. Keep this tracked template value-free.
HF_TOKEN=

# Optional local data root override. Concrete machine paths belong only in the ignored local .env.
M5DAYLOG_DATA_DIR=
