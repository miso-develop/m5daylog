# M5Daylog local component placeholder
#
# Device-private components land here in later Tasks (for example a minimal
# M5Capsule hardware abstraction where ESP-IDF drivers are insufficient).
# Task #42 keeps this directory empty on purpose: no extra dependency or
# abstraction is introduced by the scaffold itself (Decision #11).
