# M5Daylog local components
#
# Device-private components land here (for example a minimal
# M5Capsule hardware abstraction where ESP-IDF drivers are insufficient).
# Task #42 kept this directory as a placeholder with no extra dependency.
# Task #44 adds `recorder/` (PDM→DMA→SD PCM capture); see
# `recorder/README.md` for its contract. New dependencies or abstractions
# beyond the task scope are still out of bounds (Decision #11).
